#!/bin/bash

# Sync VimWiki
cd $HOME/vimwiki
git add .
git commit -m "shutdown sync"
git push

# Sync password-store
cd $HOME/.password-store
git push

# Sync TaskWarrior
task sync

# Shutdown
shutdown now
