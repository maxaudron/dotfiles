{ ... }:

{
  my = {
    desktop = {
      enable = true;
      hyprland.enable = true;
    };

    music = {
      mpd.enable = true;
      beets.enable = true;
    };

    tools = {
      printing.enable = true;
      rust.enable = true;
    };

    progs = {
      kicad.enable = true;
    };
  };
}
