{ ... }:

{
  my = {
    desktop = {
      enable = true;
      sway.enable = true;
    };

    music = {
      mpd.enable = true;
      beets.enable = true;
    };

    tools = {
      printing.enable = true;
      rust.enable = true;
    };
  };
}
